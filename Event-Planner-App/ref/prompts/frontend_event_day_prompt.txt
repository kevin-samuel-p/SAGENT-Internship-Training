On the dashboard, there is an empty section next to the list of events. Here, we should implement a mini todo-list with tasks to be submitted by the respective user in ascending order of date. Pending tasks that are beyond the due date should be marked in red (to indicate urgency), tasks to be submitted today should be marked in yellow, tasks to be submitted this week should be marked in green, and other tasks to be submitted (in the later future) will be marked gray. There should be a "Mark as Done" button near each task in the dashboard. By default, it should be a resizable list with a drop-down arrow, showing only upto 6 tasks initially. Below this (still on the right) will be the place where DMs will be implemented. I'll walk you through this later, just create a placeholder for the DMs panel underneath.

Between the header ribbon (containing the Create New Event, View Invitations, Notifications and Logout button) and the two panels (Your Events on the left, Todo list on the right), we need to implement timeline visual/schematic for the followup to the event day. It will be like a timeline, with event day sitting at 75%-80% of the timeline. It should be hoverable, with points marked for deadlines. Hovering over a point (dot) on the timeline will show a floating tag indicating a deadline (for a task). Make sure this div doesn't span too much horizontal length. If possible, create a calendar element on the right, marking these dates on the calendar for the following month. If the user tries to expand the calendar, it should open to a modal calendar, spanning from the initial time of event planning (the initiation of event) to the final day of the event.

If you have any queries, get them clarified before proceeding.